--From root run

celery -A flaskr.tareas.tareas worker -l info -P solo --Start Celery

cd flaskr && flask run -- Start python APP dev server

gunicorn -w 4 --bind 0.0.0.0:8000 wsgi:app --gunicorn server